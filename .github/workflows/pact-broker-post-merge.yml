name: Pact broker post-merge

on:
  push:
    branches:
      - master
    paths:
      - 'ci/docker/pact-broker/**'

concurrency: pact-broker-post-merge

jobs:
  tag-release:
    permissions:
      contents: write
    uses: govuk-pay/pay-ci/.github/workflows/_create-alpha-release-tag.yml@8597f9957716cb928c22ed66ee0704a24798ce6e
    with:
      tag_prefix: pactbroker_
